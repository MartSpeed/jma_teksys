/*CUSTOM EXCEPTIONS
 * [] exception class that we need to use for the correct exceptions and
 * messages that we need
 * */

package Main;

public class CustomExceptionFile extends Exception {
	// example test case
	/*
	 * private double amount; // create a method from this public
	 * CustomExceptionFile(double amount) { super(amount); this.amount = amount; }
	 * 
	 * // if you had insufficient funds in a bank account public double getAmount()
	 * { return amount; }
	 */
}
